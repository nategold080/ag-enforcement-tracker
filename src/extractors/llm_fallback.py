"""LLM fallback extractor — Phase 5.

This module will use the Anthropic API to extract structured fields
from press releases where rule-based extraction fails or returns
low-confidence results.

NOT IMPLEMENTED YET. This is a Phase 5 component.
"""


def llm_extract(text: str) -> dict:
    """Placeholder for LLM-based extraction. Phase 5."""
    raise NotImplementedError("LLM fallback extraction is not yet implemented (Phase 5).")
